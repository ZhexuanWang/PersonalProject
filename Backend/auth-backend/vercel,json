{
  "version": 2,
  "builds": [
    {
      "src": "api/index.ts",  // ← 直接使用 .ts 文件
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/api/index.ts"
    }
  ]
}